spring:
  datasource:
    url: jdbc:mysql://localhost:3306/hrms_db
    username: root
    password: 1212
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
server:
  error:
    include-stacktrace: never
  port: 8080